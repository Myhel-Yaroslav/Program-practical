#include <stdio.h>

int main() {
    float t1, t2, t3;
    printf("Введіть час за який один гість може з'їсти пиріг-");
    scanf("%f %f %f", &t1, &t2, &t3);
    printf("Час необхідний для з'дання пирога - %.2f\n", 1.0f / (1.0f / t1 + 1.0f / t2 + 1.0f / t3));
    return 0;
}
